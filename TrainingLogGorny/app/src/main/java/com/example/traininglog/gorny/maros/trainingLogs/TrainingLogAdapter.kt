package com.example.traininglog.gorny.maros.trainingLogs

import android.view.ViewGroup
import androidx.core.view.get
import androidx.recyclerview.widget.RecyclerView
import com.example.traininglog.gorny.maros.ConstraintItemViewHolder
import com.example.traininglog.gorny.maros.R
import com.example.traininglog.gorny.maros.data.TrainingLogRow
/*
class TraningLogAdapter: RecyclerView.Adapter<ConstraintItemViewHolder>() {
    var data = listOf<TrainingLogRow>()

    override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): ConstraintItemViewHolder {
        TODO("Not yet implemented")
    }

    override fun onBindViewHolder(holder: ConstraintItemViewHolder, position: Int) {
        val item = data[position]
        //holder.constraintView.getViewById(R.id.workoutType). =
        holder.constraintView.get(R.id.workoutType).
    }

    override fun getItemCount() = data.size
}

 */